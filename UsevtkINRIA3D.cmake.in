#
# This file sets up include directories, link directories, and
# compiler settings for a project to use MIPS.  It should not be
# included directly, but rather through the MIPS_USE_FILE setting
# obtained from MIPSConfig.cmake.
#


# Load the compiler settings used for MIPS.
IF(vtkINRIA3D_BUILD_SETTINGS_FILE)
  INCLUDE(${CMAKE_ROOT}/Modules/CMakeImportBuildSettings.cmake)
  CMAKE_IMPORT_BUILD_SETTINGS(${vtkINRIA3D_BUILD_SETTINGS_FILE})
ENDIF(vtkINRIA3D_BUILD_SETTINGS_FILE)

# Add compiler flags needed to use vtkINRIA3D.
SET(CMAKE_C_FLAGS   "${CMAKE_C_FLAGS} ${vtkINRIA3D_REQUIRED_C_FLAGS}")
SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${vtkINRIA3D_REQUIRED_CXX_FLAGS}")

# Add include directories needed to use MIPS.
INCLUDE_DIRECTORIES(${vtkINRIA3D_INCLUDE_DIRS})

# Add link directories needed to use MIPS.
LINK_DIRECTORIES(${vtkINRIA3D_LIBRARY_DIRS})